# URL of the deployed backend (Render), no trailing slash.
# Leave empty for local dev — Vite's proxy (vite.config.js) forwards
# /api requests to http://localhost:5000 automatically.
# Example production value: https://src262-api.onrender.com
VITE_API_URL=
